<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.cte.CteCodeMapper">

    <resultMap id="cteCodeMap" type="com.example.demo.cte.CteCode">
        <id property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="level" column="level"/>
    </resultMap>

    <select id="findAll" resultMap="cteCodeMap">
        WITH RECURSIVE category_tree(category_id, name, parent_id, level) AS (
            SELECT category_id, name, parent_id, 1 AS level
            FROM category
            WHERE parent_id IS NULL
            UNION ALL
            SELECT c.category_id, c.name, c.parent_id, ct.level + 1
            FROM category c
            INNER JOIN category_tree ct ON c.parent_id = ct.category_id
        )
        SELECT category_id, name, parent_id, level
        FROM category_tree
        ORDER BY level, category_id
    </select>

    <select id="findDescendants" resultMap="cteCodeMap">
        WITH RECURSIVE category_tree(category_id, name, parent_id, level) AS (
            SELECT category_id, name, parent_id, 1 AS level
            FROM category
            WHERE category_id = #{categoryId}
            UNION ALL
            SELECT c.category_id, c.name, c.parent_id, ct.level + 1
            FROM category c
            INNER JOIN category_tree ct ON c.parent_id = ct.category_id
        )
        SELECT category_id, name, parent_id, level
        FROM category_tree
        ORDER BY level, category_id
    </select>

    <select id="findAncestors" resultMap="cteCodeMap">
        WITH RECURSIVE category_tree(category_id, name, parent_id, level) AS (
            SELECT category_id, name, parent_id, 1 AS level
            FROM category
            WHERE category_id = #{categoryId}
            UNION ALL
            SELECT c.category_id, c.name, c.parent_id, ct.level + 1
            FROM category c
            INNER JOIN category_tree ct ON ct.parent_id = c.category_id
        )
        SELECT category_id, name, parent_id, level
        FROM category_tree
        ORDER BY level DESC
    </select>

</mapper>
